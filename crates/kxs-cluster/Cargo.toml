[package]
name = "kxs-cluster"
version = "0.1.0"
edition = "2021"

[dependencies]
kxs-core = { path = "../kxs-core" }
kube = { version = "1", default-features = false, features = ["client", "config", "runtime", "rustls-tls", "ring", "ws"] }
k8s-openapi = { version = "0.25", features = ["latest"] }
tokio = { version = "1", features = ["macros", "rt", "time", "sync"] }
futures = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml_ng = "0.10"
http = "1"
base64 = "0.22"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time", "sync"] }
